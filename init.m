% Clean up the environment
clc; clear; close all;

% Add everything in the current folder to the path
addpath(genpath(pwd));

% Declare global variable MAIN to be accessed in the workspace
global MAIN
MAIN = [];

% To run a project use the following command:
%       numeric_main("PROJECT_main")
